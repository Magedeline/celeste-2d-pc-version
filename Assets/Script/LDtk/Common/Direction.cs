using UnityEngine;

/// <summary>
/// Common direction enum used by LDtk entities (Spikes, Springs, Room Transitions, etc.)
/// </summary>
public enum Direction
{
    Up,
    Down,
    Left,
    Right
}
